# 后续开发备忘（v1.1.2「读取考评表(.doc)」功能）— 进度与待办

> 记录时间：2026-08-21
> 状态：**功能已完整实现并通过端到端验证（读取原版 + 读回本工具生成文档均 100%），仅剩打包发布与可选项优化**
> 相关版本：v1.1.2（未发布，未重新打包 exe）

---

## 一、本轮目标（用户需求原文大意）

> 读取一份已完成的考评表 `.doc`（如 `E:\2.武钢兴达工作\2.月度固定工作\2026\2026.07\履职履责\安全员安全生产责任制履职清单考评表（孙忠7月）.doc`），自动读取**评分（自评/上级）、评价（自评描述/评价描述）、支撑材料**，回填到 GUI，方便替换材料、改月份后生成新表。
> ——注意：不是读 xlsx 履职履责表评分（v1.2.0 那个方向已确认不要）。

## 二、已完成

### 2.1 核心函数 `kaoping_core.extract_kaoping_doc(doc_path, out_dir)`
- **文字读取（Word COM）**：表头 R1 解析姓名/月份（`_parse_kaoping_header`）；12 项逐行读 C6/C7/C9/C10 与 C8 文字。
- **图片提取**：`CopyAsPicture` → 剪贴板 → Pillow 存 PNG（**显示分辨率**，约 112px，作为可替换占位）。
- **OLE 内嵌文件提取（三条路径）**：
  1. **原生 Word/Excel OLE** → `OLEFormat.Object` + `SaveAs`（Word.Document 实测可用；Excel 在本机 Word COM 下取不到 Object，自动走第 2 条）；
  2. **Package 对象（`\x01Ole10Native` 流）** → 底层解析：`4 字节长度前缀 + 0200 标记 + GBK 文件名 + 内容长度(魔法字节前 4 字节) + 原始文件`（doc/docx/xls/xlsx 均实测可完整打开）；
  3. **Excel（`package` 流）** → 直接导出为 xlsx。
- **对应关系**：未被 COM 导出的 OLE 形状，按 kind（excel / word_docx / word_doc / pkg）分组后与 ObjectPool 条目顺序对应（`_ole_kind_match`）。
- **材料落盘**：`输出目录\提取材料`，Word 文档用 `项X_材料Y.doc/.docx`，Package 保留包内原始文件名。

### 2.2 GUI（`generate_kaoping.py`）
- 顶部新增「**读取考评表**」行：支持**点击选择 / 拖入** `.doc`；
- 自动回填：姓名、月份（源表头有才覆盖）、12 项全部字段 + 支撑材料（缩略图/文件图标）；
- 状态栏显示「已读取 XXX 考评表：12 项内容 + N 个支撑材料」；有警告时弹窗提示；
- 已移除 v1.2.0 的 xlsx「读取评分」UI（`read_eval_scores` 保留在核心层备用）。

### 2.3 依赖与环境
- **olefile 0.47** 已装入 `venv38x64` 与 `venv38x86`；`安全员履职考评表生成工具.spec` 的 `hiddenimports` 已加 `olefile`；
- README / 使用说明.md / 技术方案.md 已同步（版本标注 v1.1.2）。

### 2.4 测试
- `tests/test_core.py`：**12/12 通过**（新增 `test_parse_kaoping_header`、`test_extract_ole10native`、`test_safe_filename`）；
- `tests/test_gui.py`：**9/9 通过**（布局测试更新为 `import_label`/「读取考评表」，新增 mock 版回填测试）。

## 三、实测结果（用真实文件验证过）

| 场景 | 结果 |
|------|------|
| 读取**原版**孙忠7月.doc（用户手工/旧版填写，Package+Ole10Native 格式） | ✅ 姓名=孙忠；12 项评分/评价/材料文字全部正确；**12 个支撑材料全部提取成功**（4 图 + 5 Package + 2 Excel + 1 docx 等） |
| 往返：读 7月 → 生成 8月 → **再读 8月（本工具输出）** | ✅ **已 100% 解决**：Word 文档对象用 `OLEFormat.Object`+SaveAs 导出；Excel 用 IconLabel 命名 + **Excel 条目反转对应**（本工具生成文档的 ObjectPool 中 Excel 条目与形状顺序相反，多份实测一致）；内容核对（sheet 名）4 个 Excel 全部对应正确 |

## 四、遗留问题（下次继续的重点）

1. **【已解决】读取本工具生成的考评表**：原版/手工文档（Package 格式）正序对应；本工具输出（含原生 Word OLE）Excel 条目在 ObjectPool 中**反转对应**（3 份独立生成文档实测一致）+ IconLabel 命名，内容核对全部正确。不再需要用户确认读取范围。
2. **图片分辨率**：当前导出为显示分辨率（约 112px），作为占位参考够用；如需原分辨率需从 .doc 的 `Data` 流解析（较复杂，一般没必要）。
3. **材料落盘目录**：固定为「输出目录\提取材料」，多次读取会混在一起；建议后续按 `姓名+月份` 建子目录，避免覆盖/混淆。
4. **打包发布**：以上改动均在源码层，**exe 尚未重新构建**。执行：
   ```bash
   python _build_win7.py                                    # 32 位版
   $env:PYI_BITS="64"; $env:PYI_EXE_NAME="安全员履职考评表生成工具"; python _build_win7.py   # 64 位版
   ```

## 五、当前未提交的变更（git status）

```
M  generate_kaoping.py
M  kaoping_core.py
M  tests/test_core.py
M  tests/test_gui.py
M  使用说明.md
M  安全员履职考评表生成工具.spec
M  技术报告_安全员履职考评表生成工具.md   ← 此前加的"历史记录"提示
?? README.md                               ← 此前新建
?? 技术方案.md                             ← 此前新建
```

> 说明：README.md 与 技术方案.md 是更早几轮（v1.1.1 尺寸修复、v1.2.0 xlsx、v1.3.0 读 .doc）一起积累的文档，均未提交；发布版本号统一定为 v1.1.2。

## 六、技术要点备忘（供下次快速上手）

- **孙忠7月.doc 结构**：16×10 表；表头 R1；12 项 R3/R5~R15；C6 自评描述、C7 自评得分、C8 材料、C9 评价描述、C10 上级评分。
- **原版文档 OLE 格式**：Package 对象存于 `ObjectPool/<name>/\x01Ole10Native`（内含原始文件）；Excel 存于 `ObjectPool/<name>/package`（即 xlsx 本身）。
- **本工具输出文档 OLE 格式**：.doc → Word.Document.8（原生 `WordDocument` 流）；.docx → Word.Document.12；.xlsx → Excel.Sheet.12（`package` 流）。
- **OLE 形状 ↔ ObjectPool 对应规律**：
  - 原版文档（全 Package/Excel package 流）：**正序**对应；
  - 本工具输出（含原生 Word OLE）：Excel 条目在 ObjectPool 中与形状顺序**相反**（多份实测一致）→ 检测到 `word_native`/`word_docx` 条目即反转。
- **IconLabel**：本工具生成的 OLE 对象 `IconLabel` = 原始文件名（`AddOLEObject(IconLabel=文件名)`），读取时用于精确命名。
- **原生 Word OLE 导出**：`OLEFormat.Object` + `SaveAs2`（Word.Document.8→.doc 格式0，Word.Document.12→.docx 格式12）；Excel 取 Object 会 E_NOINTERFACE，改用 ObjectPool `package` 流。
- **Ole10Native 解析要点**：前 4 字节=整段长度（=流长-4）；然后 `0200`；GBK 文件名（NUL 结尾）；源路径（NUL 结尾）；内容长度（魔法字节 `PK\x03\x04` / `D0CF11E0` 前 4 字节）；内容；尾部临时路径（UTF-16）。
- **图片提取**：`shp.Range.CopyAsPicture()` → `PIL.ImageGrab.grabclipboard()` → 存 PNG。
- **office 类型识别**：`_sniff_office_kind` 按 zip 内 `xl/`（xlsx）或 `word/`（docx）判断，比依赖 CompObj ProgID 更可靠（原版文档的 Excel CompObj 可能不带 "Excel" 字样）。

## 七、下一步建议（优先级排序）

1. **重新打包 exe（32/64 位）** 并给用户分发测试（v1.1.2，含尺寸修复 + 读取考评表）。
2. 材料目录按 `姓名+月份` 建子目录（可选优化）。
3. 多岗位扩展（作业长/班组长/主要负责人）按《技术方案.md》第四章推进。
