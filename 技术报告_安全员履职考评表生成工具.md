# 《安全员安全生产责任制履职清单考评表》自动生成工具 — 技术报告（v0.1）

> 版本：v0.1（评审稿）
> 日期：2026-08-20
> 状态：待评审确认，确认后再进入代码实现
>
> ⚠️ **历史记录**：本文为项目初期评审稿（v0.1）。代码已实现并交付（v1.1.0），正式技术方案与多岗位扩展设计见 [技术方案.md](技术方案.md)。

---

## 一、项目目标

依据空白模板自动生成 **「安全员安全生产责任制履职清单考评表(X月XXX).doc」**，实现：

1. **图形化界面**：12 个考评项（对应表格 12 行）逐一提供"自评描述 / 自评得分 / 自评实绩材料（拖拽图片）/ 评价描述 / 上级评分"录入与拖拽区；
2. **图片拖拽 + 缩略图预览**：每个考评项可拖入 1~N 张图片（jpg/png/bmp 等），界面即时预览；
3. **一键生成**：自动填入文本、插入图片、自动计算合计分，按命名规则输出 `.doc` 文件；
4. 参考成熟项目 **《中夜班、节假日安全消防检查情况快速制作》** 的架构（tkinter + tkinterdnd2 + PyInstaller 单 EXE）。

---

## 二、现状调研结果

### 2.1 考评表模板结构（已用 Word COM 实际解析 3/5/7 月三份样本）

- **文件格式**：`.doc` 二进制格式（OLE2）。**注意：`安全员安全生产责任制履职清单考评表（孙忠4月）.docx` 扩展名为 docx，但文件头 `D0 CF 11 E0 A1 B1 1A E1` 实为 .doc 二进制，python-docx 无法直接打开**。输出目标 `.doc` 亦为二进制格式。
- **表格结构**：全文只有 1 张表，**16 行 × 10 列**（网格坐标），存在纵向合并单元格（第 1 项"安全绩效"占 R3+R4 两行），因此**只能用 `Table.Cell(行,列)` 网格坐标访问，不能用 `Table.Rows(i)` 按行访问**。

| 行 | 含义 | 可填内容 |
|----|------|----------|
| R1 | 表头合并格：`考评对象（安全员）：孙忠 管理者姓名：（手签）评价月份：5月 评价人员签字：（手签）` | 姓名、月份（字符串替换） |
| R2 | 列标题 | 固定 |
| R3 | ① 安全绩效（标准分20） | C6 自评描述 / C7 自评得分 / C8 实绩材料 / C9 评价描述 / C10 上级评分 |
| R4 | ① 续行（合并单元格） | — |
| R5 | ② 管理体系（10） | 同上 |
| R6 | ③ 教育培训（5） | 同上 |
| R7 | ④ 会议活动（5） | 同上 |
| R8 | ⑤ 检查改进（5） | 同上 |
| R9 | ⑥ 应急演习（5） | 同上 |
| R10 | ⑦ 监督执行（5） | 同上 |
| R11 | ⑧ 安全能力（5） | 同上 |
| R12 | ⑨ 危险源（5） | 同上 |
| R13 | ⑩ 隐患排查（5） | 同上 |
| R14 | ⑪ 违章管理（20） | 同上 |
| R15 | ⑫ 其它（10） | 同上 |
| R16 | 合计 | C6 自评合计、C9 评价合计（自动求和） |

- **固定内容**：C4（履职清单）、C5（扣分标准）为模板固定文字，工具不改动。
- **图片规格（实测）**：C8（自评实绩材料）列宽约 **2.9cm**，样本中插入的内嵌图片宽约 **2.3~2.6cm**，高约 1.9~2.3cm，单个单元格最多出现过 **2 张图**（7月 R14C8 有 2 张）。
- **字体（实测）**：可填单元格为 **宋体 10pt、居中**；表头行为 **黑体 12pt**。
- **自评实绩材料单元格（C8）既可放图片，也可放文字**（如 R3C8"未发生事故"、R11C8"已取证"）。

### 2.2 关联数据（履职履责表 xlsx）

- 各月文件夹内有 `XX月炼铁保产事业部履职履责表（兴达）.xlsx`，其中 **「安全员月度履职评价表」** sheet 已含权威打分与扣分说明，12 个维度与考评表 12 行一一对应：

| 列 | 维度 | 标准分 |
|----|------|--------|
| E | 安全/绩效 | 20 |
| G | 管理体系 | 10 |
| I | 教育培训 | 5 |
| K | 会议活动 | 5 |
| M | 检查改进 | 5 |
| O | 应急演习 | 5 |
| Q | 监督执行 | 5 |
| S | 安全能力 | 5 |
| U | 危险源 | 5 |
| W | 隐患排查 | 5 |
| Y | 违章管理 | 20 |
| AA | 其它 | 10 |
| AC | 总分 | 100 |

- 该表同时含 **4 名安全员**（孙忠/事业部、王旺/煤库、王雪梅/供矿、胡明/原料），可支持一人或批量生成。

### 2.3 参考项目技术栈（《中夜班快速制作》）

- `generate_reports.py`（GUI：tkinter + tkinterdnd2 拖拽）+ `report_core.py`（python-docx 填表逻辑）+ `模板.docx`；
- PyInstaller 单 EXE 打包（spec 中 `datas` 携带模板，`hiddenimports` 含 tkinterdnd2）。
- **差异**：参考项目模板是真正的 `.docx`，可用 python-docx；本项目模板/输出是 `.doc` 二进制，**python-docx 无法胜任**，需改用 Word COM。

### 2.4 运行环境（实测确认）

| 项 | 状态 |
|----|------|
| Python 3.13.13 (D:\Python\Python313) | ✅ |
| python-docx / openpyxl / xlrd | ❌ 未使用（.doc 为 OLE2 二进制，需 Word COM；openpyxl 依赖已随 v1.0.4 移除） |
| tkinter / tkinterdnd2 | ✅ |
| pywin32 + **Microsoft Word 16.0（COM 可用）** | ✅ |
| PyInstaller 6.21.0 | ✅ |
| 工作目录 | 空目录，可新建工程 |

---

## 三、总体方案

### 3.1 技术选型（核心决策）

| 方案 | 说明 | 结论 |
|------|------|------|
| **A. Word COM（win32com）直接填 .doc** | 打开模板 .doc → `Table.Cell(r,c)` 填文本/`InlineShapes.AddPicture` 插图 → 另存 .doc | ✅ **推荐**：格式保真、一步到位、无转换损耗；前提是目标机器装有 Word（已确认本机可用） |
| B. 模板转 docx + python-docx + 再转回 .doc | 需 Word COM 做转换兜底，两套代码、链路长 | ❌ 不推荐 |

### 3.2 模块划分（对齐参考项目）

```
安全员履职考评表生成工具/
├─ generate_kaoping.py      # 入口 + GUI（tkinter + tkinterdnd2）
├─ kaoping_core.py          # 核心逻辑：模板定位、文本填充、图片插入、合计计算、保存
├─ 安全员安全生产责任制履职清单考评表（模板）.doc   # 空白模板（已由用户提供，位于根目录）
├─ 安全员履职考评表生成工具.spec         # PyInstaller 配置
└─ build/ dist/             # 打包产物
```

### 3.3 GUI 界面设计

- 窗口约 1200×850，顶部分四块：
  1. **基本信息**：年份/月份、姓名（手填）、命名模板（默认 `安全员安全生产责任制履职清单考评表(X月XXX).doc`）、模板文件（默认自动定位）、输出目录（记忆上次路径）；
  2. **12 个考评项列表**（可滚动）：每项含「自评描述」输入框、「自评得分」输入框（默认=标准分）、「支撑材料」拖拽槽（**图片/文档/表格等任意文件，每格最多 15 个**，图片缩略图预览、文件显示文件名、双击打开/右键删除）；
  3. **底部工具栏**：进度条、「清空全部」、「一键生成」（最先布局并靠右，窗口缩小时不被遮挡）。
- 材料拖入后即时预览（图片缩略图 / 📄文件名）；生成完成弹窗提示并支持"打开文件/打开文件夹"。

### 3.4 生成流程

1. 校验模板（`安全员安全生产责任制履职清单考评表（模板）.doc`）与必填项（姓名、月份）→ 用副本操作，**不污染模板原件**；
2. Word COM 打开模板副本；
3. 替换 R1 表头中的姓名、月份；
4. 逐项写入 C6（自评描述）、C7（自评得分）、C8（先清空原内容，再依次写入文字与支撑材料）；C9（评价描述）、C10（上级评分）可留空待手签；
5. 支撑材料插入规则：**图片→内嵌图片（宽≈2.5cm 等比）；其他文件→OLE 嵌入对象（图标+文件名，双击打开源文件）；嵌入失败退化为文件名文字**；
6. 自动求和 C7 → R16C6、C10 → R16C9；
7. 按命名模板生成文件名（默认 `安全员安全生产责任制履职清单考评表(X月XXX).doc`）另存至输出目录；
8. 生成完成后弹窗提示，可"打开文件/打开所在文件夹"。

### 3.5 打包

- PyInstaller 单 EXE：`datas` 携带模板；`hiddenimports=['tkinterdnd2']`；`console=False`；
- 与参考项目一致，双击即用；**要求目标机装有 Microsoft Word**。

---

## 四、发现的问题与待确认事项（请在评审时逐一回复）

### Q1【✅ 已确认】空白模板
您已提供：根目录 `安全员安全生产责任制履职清单考评表（模板）.doc`（58KB，无图片）。
解析确认：16行×10列，与已填样本结构完全一致；表头残留"孙忠"、评价月份为空；12 个考评项的 C6/C7/C9/C10 残留 7 月文字（程序将逐格覆盖，不影响使用）。

### Q2【✅ 已确认】输出 `.doc` 依赖 Microsoft Word COM
采用方案 A（Word COM），本机 Word 16.0 已验证可用；目标机器需装有 Microsoft Word。

### Q3【✅ 已确认】接入 xlsx 自动填充
GUI 增加「读取履职履责表.xlsx」按钮：自动填入 12 项**自评得分 + 自评描述（扣分说明）**；图片仍手动拖拽。表结构按「安全员月度履职评价表」sheet 列 E/G/I/K/M/O/Q/S/U/W/Y/AA 映射至 12 项，AC 为总分。
> **变更记录**：按用户要求已**移除「读取履职履责表.xlsx」自动填充 UI**（顶部拖拽区 + 底部按钮），姓名改手填；`read_xlsx_scores` 与 `XLSX_*` 工作表映射及 openpyxl 依赖已从 `kaoping_core.py` 彻底删除。

### Q4【✅ 已确认】单人模式
每次生成 1 人 1 份；姓名手填或从 xlsx 读取后下拉选择；架构预留批量扩展。

### Q5【✅ 已确认】文件命名自由配置
界面提供命名模板输入框，默认 `安全员安全生产责任制履职清单考评表(X月XXX).doc`（X=月份，XXX=姓名）；支持自定义（如改全角括号、姓名在前）。

### Q6【✅ 已确认（补充）】自评 + 上级评分
界面提供「自评描述 + 自评得分 + 自评实绩材料（文字+图片）+ 评价描述 + 上级评分」；评价列可留空待手签，也可录入。
**格式保护硬性要求**：所有文本写入采用"单元格 Range 文本替换"方式，保留模板原 Run 的字体/段落格式，禁止重建段落；图片仅以内嵌方式插入 C8。

### Q7【✅ 已确认】预览形式
图片拖入后即时显示**缩略图预览**；生成完成弹窗提示，可点击"打开文件/打开所在文件夹"。

### Q8【✅ 已确认（升级）】支撑材料（图片+文件）
- 每格支持 **0~15 个任意文件**（图片/Word/Excel/PDF 等），不再限 3 个；
- **预览**：图片显示缩略图，其他文件显示 📄 文件名；双击打开源文件、右键删除单个，悬停显示完整文件名；
- **生成**：图片→内嵌图片（宽≈2.5cm 等比）；其他文件→**OLE 嵌入对象**（图标+文件名，双击打开源文件），嵌入失败退化为文件名文字；
- 工作流升级：**拖入/选择当月文件夹或年度根目录**（选年度根目录时按界面年月自动定位当月子文件夹），按本地**文件名权重**（`material_rules.json`，可用「编辑权重」按钮调整）自动匹配并排序 → 逐项替换支撑材料 → 一键生成。

### Q9【✅ 已确认】输出目录
记忆上次使用路径；首次默认输出到程序所在目录。

### Q10【✅ 已确认（补充）】时间（年/月）处理
- 界面增加「年份」输入（默认当前年）与「月份」输入；
- 命名模板支持占位符：`{Y}`=年、`{X}`=月、`{XXX}`=姓名（默认 `安全员安全生产责任制履职清单考评表({X}月{XXX}).doc`）；
- 表头「评价月份：」按所选月份填写；
- xlsx 履职履责表按姓名匹配，与年份/月份无强耦合（用户手动选择文件）。

---

## 五、风险与注意事项

1. **格式保真**：通过"替换已有 Run 文本 + 保留原段落格式"方式填充，避免新建 Run 导致字体变化（参考 `_fill_cell_text` 思路的 COM 版）。
2. **纵向合并单元格**：只能用 `Cell(r,c)` 网格坐标，禁止 `Rows(i)`；合并导致的行访问异常已在调研中复现，须在代码中 try/except 兜底。
3. **图片撑高行**：插入 2 张图时行会自动变高，属于正常现象；建议图片宽统一 ≤2.5cm 以控制页数。
4. **Word 进程残留**：生成过程异常时须 `finally` 中 `Close`/`Quit`，避免 Word 进程卡死。
5. **模板只读保护**：建议生成前复制模板到临时文件，绝对不写回模板原件。
6. **打包体积**：参考项目 EXE 约 36MB，本项目预计相近。
7. **中文编码**：源码文件一律 UTF-8 + `# -*- coding: utf-8 -*-`，运行设置 `PYTHONUTF8=1`，避免 Windows GBK 控制台乱码。

---

## 六、里程碑

| 阶段 | 内容 | 状态 |
|------|------|------|
| M1 | 本报告评审，确认 Q1~Q10 | ✅ 已确认（Q1 模板已提供；Q2~Q10 均已答复） |
| M2 | `kaoping_core.py`：Word COM 填充核心（文本/图片/合计/保存） | ✅ 完成，真实数据端到端验证通过 |
| M3 | `generate_kaoping.py`：GUI（12 项表单 + 拖拽 + 预览 + 文件名权重匹配 + 滚轮 + 一键生成 + 后台扫描/进度/日志） | ✅ 完成，冒烟测试通过 |
| M4 | 用 5 月真实数据端到端验证，与人工件对比 | ✅ 完成（结构/字体/图片/合计全部正确） |
| M5 | PyInstaller 打包单 EXE + 使用说明 | ✅ 完成：`dist\安全员履职考评表生成工具.exe`（37MB，启动验证通过） |

**交付物清单**：
- `generate_kaoping.py` — 主程序（GUI，双击源码运行）
- `kaoping_core.py` — 生成核心（Word COM 填充）
- `安全员安全生产责任制履职清单考评表（模板）.doc` — 空白模板（只读）
- `安全员履职考评表生成工具.spec` — PyInstaller 打包配置
- `dist\安全员履职考评表生成工具.exe` — 免安装单文件（最终交付）
- `使用说明.md` — 用户操作手册
