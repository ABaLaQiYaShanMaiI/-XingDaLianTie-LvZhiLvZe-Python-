# 《安全员安全生产责任制履职清单考评表》自动生成工具 — 技术方案（v1.1）

> **版本**：v1.1（定稿）　**日期**：2026-08-21　**状态**：✅ 已实现交付（对应代码 v1.1.4）
> **定位**：本文档是项目的正式技术方案，同时作为**多岗位扩展（作业长 / 班组长 / 主要负责人履职履责工具）**的基线设计，后续岗位工具按第四章参数化模型派生。

---

## 一、概述

### 1.1 背景

每月安全员履职考评工作需在《安全员安全生产责任制履职清单考评表》上人工填写 12 个考评项的自评与上级评价，并粘贴大量支撑材料（图片/文档），重复且易错。本项目以甲方空白模板为唯一版式来源，通过图形化界面录入、支撑材料文件夹自动匹配、Word 引擎填充，一键生成与手工件完全一致的 `.doc` 文件。

### 1.2 目标

1. 图形化录入 12 个考评项（自评描述 / 自评得分 / 材料说明 / 支撑材料 / 评价描述 / 上级评分）；
2. 支撑材料文件夹按文件名权重自动匹配到各考评项；
3. 一键生成 `.doc`，输出与手工件版式完全一致（格式保真）；
4. 免安装单 exe 分发，兼容 Windows 7 SP1 ~ Windows 11（32/64 位）；
5. **架构可复用**：将"岗位专属参数"从代码中抽象出来，未来派生作业长、班组长、主要负责人等岗位工具。

### 1.3 术语约定

| 术语 | 含义 |
|------|------|
| 考评项 | 表格中的 12 个考评维度（①~⑫） |
| 支撑材料 | 每个考评项 C8 列的实绩材料（图片/文档/表格/照片等） |
| 岗位参数 | 描述一个岗位所需的所有定制项（模板、考评项、行映射、命名、权重等） |
| C6/C7/C8/C9/C10 | 考评表列坐标：自评描述 / 自评得分 / 实绩材料 / 评价描述 / 上级评分 |

---

## 二、需求与现状调研（实测结论）

### 2.1 考评表模板结构（已用 Word COM 实际解析多份样本）

- **格式**：`.doc` 二进制（OLE2）。注意「孙忠4月.docx」虽名为 .docx，文件头 `D0 CF 11 E0 A1 B1 1A E1` 实为 .doc，python-docx 无法打开；输出目标 `.doc` 同为二进制；
- **表格**：全文仅 1 张表，**16 行 × 10 列**（网格坐标）；第 1 项「安全绩效」纵向合并占用 R3+R4 两行；
- **坐标访问约束**：只能 `Table.Cell(行,列)` 网格坐标访问，**禁止 `Table.Rows(i)`**（合并单元格会导致按行访问异常，已实测复现）；
- **行结构**：

| 行 | 含义 | 可填内容 |
|----|------|----------|
| R1 | 表头：`考评对象（安全员）：XX　管理者姓名：（手签）　评价月份：X月　评价人员签字：（手签）` | 姓名、月份 |
| R2 | 列标题 | 固定 |
| R3 | ① 安全绩效（标准分 20） | C6 自评描述 / C7 自评得分 / C8 实绩材料 / C9 评价描述 / C10 上级评分 |
| R4 | ① 续行（合并单元格） | — |
| R5~R15 | ②~⑫ 考评项（标准分 10/5/5/5/5/5/5/5/20/10） | 同上 |
| R16 | 合计 | C6 自评合计、C9 上级合计 |

- **固定内容**：C4（履职清单）、C5（扣分标准）为模板固定文字，程序不改动；
- **图片规格（实测）**：C8 列宽约 2.9cm，样本图片宽 2.3~2.6cm、高 1.9~2.3cm，单格最多 2 张 → 程序统一控制长宽 ≤ 2cm；
- **字体（实测）**：可填单元格宋体 10pt 居中，表头行黑体 12pt；C8 既可放图片也可放文字。

### 2.2 关联数据（履职履责表 xlsx，预留扩展）

各月文件夹内含《XX月炼铁保产事业部履职履责表（兴达）.xlsx》，其中「安全员月度履职评价表」sheet 的 12 列与 12 个考评项一一对应（E→①安全绩效20、G→②管理体系10、I→③教育培训5、K→④会议活动5、M→⑤检查改进5、O→⑥应急演习5、Q→⑦监督执行5、S→⑧安全能力5、U→⑨危险源5、W→⑩隐患排查5、Y→⑪违章管理20、AA→⑫其它10、AC→总分100）。

> ✅ **v1.1.2 已实现**：界面新增「读取考评表」行，选择已生成的考评表(.doc) 后，读取姓名/月份、12 项自评与上级评价、支撑材料（图片剪贴板导出 + OLE 内嵌文件 olefile 底层还原），回填界面便于替换后生成新月份。解析逻辑见 3.4.12。

### 2.3 运行环境（实测确认）

| 项 | 结论 |
|----|------|
| 目标系统 | Windows 7 SP1 ~ Windows 11（32 / 64 位） |
| 运行依赖 | Microsoft Word 2010+（COM）；Word 2007 有 SaveAs 兜底 |
| 开发环境 | Python 3.8.10（官方支持 Win7 的最后版本） |
| 关键库 | pywin32 306（Word COM）、pillow 10.4.0（缩略图/图片尺寸）、tkinterdnd2 0.4.3（拖拽）、PyInstaller 5.13.2 |
| 不用库 | python-docx / openpyxl / xlrd（.doc 为 OLE2，Word COM 已覆盖） |

### 2.4 关键技术约束

1. 模板必须只读，生成时使用副本操作；
2. 输出 `.doc` 格式保真，不能用 python-docx 重建；
3. 目标机器可能为未打 KB2999226 补丁的纯净 Win7 → exe 需内置 UCRT；
4. 部分环境动态分派 COM 会挂起 → 优先静态类型库分派（`gencache.EnsureDispatch`）。

## 三、总体技术方案

### 3.1 技术选型（核心决策）

| 方案 | 说明 | 结论 |
|------|------|------|
| **A. Word COM（win32com）直接填 .doc** | 打开模板副本 → `Cell(r,c)` 填文本 / `InlineShapes.AddPicture` 插图 / `AddOLEObject` 嵌对象 → `SaveAs2` 另存 .doc | ✅ 采用：格式保真、一步到位、无转换损耗 |
| B. 模板转 docx + python-docx + 再转回 .doc | 需 Word COM 做转换兜底，两套代码、链路长、格式损耗风险 | ❌ 不采用 |

### 3.2 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│ GUI 层　generate_kaoping.py（tkinter + tkinterdnd2）           │
│ 基本信息区 / 12 考评项表单 / 材料拖拽预览 / 权重编辑 / 一键生成   │
│ 后台线程 + pythoncom.CoInitialize + root.after 回主线程更新     │
└───────────────┬─────────────────────────────────────────────┘
                │ items = {1..12: {desc, score, material_text,
                │          materials[], eval_desc, super_score}}
┌───────────────▼─────────────────────────────────────────────┐
│ 核心层　kaoping_core.py（win32com Word COM）                   │
│ find_template / build_filename / scan_materials_folder        │
│ generate_doc（校验/表头替换/逐项填充/插图/OLE/合计/缩放/保存）   │
└───────────────┬─────────────────────────────────────────────┘
                │ 读写
┌───────────────▼─────────────────────────────────────────────┐
│ 外部资源                                                      │
│ 空白模板(.doc) / material_rules.json / kaoping_config.json    │
│ kaoping.log / 支撑材料文件夹（当月或年度根目录）                │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 模块职责

#### 3.3.1 kaoping_core.py（核心层，纯函数 + Word COM，无 GUI 依赖）

| 函数 | 职责 |
|------|------|
| `find_template` | 定位模板：手动指定 > exe/脚本目录 > 上一级目录 > `_MEIPASS`（兼容旧版内置） |
| `build_filename` | 命名模板替换 + Windows 非法字符/结尾点空格/保留设备名消毒 |
| `ensure_material_rules_file` / `load_material_rules` | `material_rules.json` 生成与读取，文件缺失/非法回退内置规则 |
| `score_materials_file` | 文件名权重打分：相对路径（含子文件夹名）子串匹配，每项首词权重 2、其余 1 |
| `find_month_subfolder` | 年度根目录自动定位当月子文件夹（`2026.08` / `2026.8` / `2026-8` / `8月` 等） |
| `scan_materials_folder` | 递归扫描 + 权重匹配 + 按分排序 + 未匹配/超限清单 |
| `read_eval_scores` | （备用）读取月度履职履责表 xlsx：按姓名匹配「安全员月度履职评价表」sheet，返回 12 项评分/扣分说明/总分 |
| `extract_kaoping_doc` | 读取已生成考评表 .doc：姓名/月份、12 项自评与上级评价、支撑材料（图片/OLE 内嵌文件）提取到 out_dir |
| `generate_doc` | 生成主流程：打开模板副本 → 校验 → 填充 → 另存 .doc |
| `_fill_header` / `_replace_slot` | 表头姓名/月份按标签区间替换，保留空白占位并居中 |
| `_set_cell_text` / `_clear_cell_content` | 单元格 Range 文本替换（保留格式）/ 清空对象与文字 |
| `_insert_image` / `_insert_ole` / `_fit_inline_size` | 支撑材料插入与尺寸控制 |
| `_sum_scores` | 自评/上级得分合计（忽略非数值） |
| `_new_word_app` | Word 实例创建：静态类型库分派优先，防动态分派挂起 |

#### 3.3.2 generate_kaoping.py（GUI 层）

- `ImageItem`：单个考评项录入控件——两行输入（自评描述/得分/材料说明 + 评价描述/上级评分）、材料拖拽区（DND）、预览区（图片缩略图 / 📄 文件图标）、材料增删/打开/悬停提示；
- `App`：整体界面布局（顶部基本信息、中部 12 项滚动列表、底部工具栏）、模板/目录选择与拖入、支撑材料文件夹后台扫描、输入校验、一键生成（后台线程 + 进度回调）、配置读写、日志。

### 3.4 关键设计要点

#### 3.4.1 模板定位与只读保护
模板**外置**、不内置进 exe（分发时与 exe 同目录）；查找顺序：手动指定 > exe/脚本目录 > 上一级 > `_MEIPASS`（兼容旧版）。生成时打开的是模板文件本体，内容全部覆盖写入后另存新文件，模板原件不被改写。保存前校验表格结构（16 行 × 10 列）与表头关键词（含「考评对象」「评价月份」），防止模板被换后错格写入。

#### 3.4.2 表格坐标访问
行/列映射集中定义：`ITEM_ROWS = {1:3, 2:5, …, 12:15}`、`TOTAL_ROW=16`、`HEADER_ROW=1`、`COL_SELF_DESC=6 / COL_SELF_SCORE=7 / COL_MATERIAL=8 / COL_EVAL_DESC=9 / COL_EVAL_SCORE=10`。一律 `tb.Cell(r, c)` 网格坐标访问，天然兼容纵向合并单元格（第 1 项跨 R3+R4）。

#### 3.4.3 文本填充（格式保真）
`_set_cell_text` 采用 Range 文本替换：取 `cell.Range`，`End-1` 排除单元格结束符 `\x07`，整体赋值 `Range.Text`，**不重建段落、不新增 Run**，保留模板原有字体/段落/对齐格式。C8 先 `_clear_cell_content`（删内嵌对象 + 清文字）再写入材料文字与对象。

#### 3.4.4 表头占位替换
`_replace_slot(body, 前标签, 后标签, 值)` 按标签区间截取：区间原值存在 → 新值顶替、首尾空白不动；区间全空白 → 空白对半分、值居中写入。姓名用「考评对象（安全员）：…管理者姓名」区间，月份用「评价月份：…评价人员签字」区间（值带"月"）。比正则组引用更稳，且保持手工件"值居中"观感。

#### 3.4.5 支撑材料插入
- **图片**：`InlineShapes.AddPicture` 内嵌；插入前用 PIL 读原图尺寸，按 `IMG_MAX_SIZE_CM = 2.0` 等比计算目标宽高后赋值（长宽均 ≤ 2cm）；
- **体积控制（v1.1.1）**：图片边长超过 `MAX_EMBED_PX = 1600` 时，先由 PIL 等比压缩为临时文件再嵌入（显示尺寸不变），避免手机照片撑大 `.doc` 体积；临时目录随生成结束清理；
- **其他文件**：`AddOLEObject(FileName, DisplayAsIcon=True, IconLabel=文件名)` 嵌入为 OLE 对象（Word 标准「插入对象」效果，双击可打开源文件）；
- **兜底**：任一插入抛异常 → 退化为 `Range.Text = 文件名`（文字）；
- **尺寸二次兜底**：保存前遍历 C8 全部 `InlineShapes` 执行 `_fit_inline_size`——图片/Excel 类 OLE 直接赋 Width/Height；Word 文档类 OLE 忽略宽高赋值，改用 `ScaleWidth/ScaleHeight` 百分比；
  - **关键守卫（v1.1.1 修复）**：`_fit_inline_size` 先经 `_fit_size` 按 `FIT_TOLERANCE_PT = 0.5` 容差判断——对象已在 2cm 内（含 Word 浮点尾差，如 56.700001pt）必须直接返回；否则再次 `ScaleWidth/ScaleHeight = 100` 会把已缩小的图片/OLE 放大回原始大小，撑爆表格版式（曾实测 486×864pt）。

#### 3.4.6 合计计算
`_sum_scores` 遍历 12 项对 `score` / `super_score` 求和，忽略非数值；无有效值返回空串；结果写入 R16C6（自评合计）、R16C9（上级合计）。

#### 3.4.7 文件命名
`build_filename` 支持占位符 `{Y}/{年}`、`{X}/{月份}`、`{XXX}/{姓名}`；随后做 Windows 消毒：非法字符 `\/:*?"<>|` → `_`、去掉主干结尾点/空格、保留设备名（CON/PRN/AUX/NUL/COM1~9/LPT1~9）前缀加 `_`；默认模板 `安全员安全生产责任制履职清单考评表({X}月{XXX}).doc`，界面可自定义。

#### 3.4.8 文件名权重匹配
`material_rules.json` 结构 `{"1": ["安全绩效","绩效",…], …}`：首词权重 2、其余权重 1，按相对路径（含子文件夹名、去扩展名）子串匹配打分；分高优先、同分取编号小者。扫描跳过：`~$` 临时文件、含「考评表/履职履责表」等自身文件名、`SKIP_EXTS`（.lnk/.url/.ini/.tmp/.db/.py/.pyc/.md）。每项上限 15 个，超限文件计入未匹配清单提示用户手动处理。

#### 3.4.9 当月文件夹定位
选年度根目录时，`find_month_subfolder` 自动定位当月子文件夹（识别 `2026.08`、`2026.8`、`2026-8`、`2026年8月`、`8月` 等命名），未找到则原样返回所选目录。

#### 3.4.10 配置与日志
- `kaoping_config.json`：记忆输出目录、命名模板（GUI 每次生成后写回）；
- `material_rules.json`：首次运行自动生成，GUI「编辑权重」用记事本打开编辑，保存后重启生效；
- `kaoping.log`：`FileHandler(encoding="utf-8")` 显式指定编码（`basicConfig` 的 `encoding` 参数 Python 3.9 才支持，Win7 构建用 3.8 会抛错）。

#### 3.4.11 并发与 Word 生命周期
- 文件夹扫描、生成均在**后台线程**执行，线程内 `pythoncom.CoInitialize()`；用 `root.after` 回主线程更新 UI，海量文件扫描不卡界面（状态栏「扫描中…」）；
- Word：`gencache.EnsureDispatch` 静态类型库分派优先（动态分派在无界面环境挂起，已实测复现），打包环境自动回退；`DisplayAlerts = 0`；`finally` 中 `doc.Close(False)` + `word.Quit()`，杜绝 Word 进程残留；
- 失败处理：`logging.exception` 写日志 + 主线程弹窗明确报错，不静默。

#### 3.4.12 已生成考评表(.doc) 读取复用（v1.1.2）
`extract_kaoping_doc(doc_path, out_dir)` 把一份已完成的考评表读回 GUI 复用（评分/评价/支撑材料）：
- **文字读取（Word COM）**：表头 R1 按标签区间解析姓名/月份（`_parse_kaoping_header`）；12 项逐行读 C6/C7/C9/C10 与 C8 文字（去 `\x07/\r/\x01`）；
- **图片提取**：`CopyAsPicture` → 剪贴板 → Pillow 存 PNG（显示分辨率，作为可替换占位）；
- **OLE 内嵌文件提取（三条路径）**：
  1. **原生 Word/Excel OLE** → `OLEFormat.Object` + `SaveAs`（Word.Document.8→.doc 格式0、Word.Document.12→.docx 格式12；Excel 取 Object 在本机 Word COM 下 E_NOINTERFACE，自动走第 2/3 条）；
  2. **Package 对象（`\x01Ole10Native` 流）** → 按「4 字节长度 + 0200 标记 + GBK 文件名 + 内容长度(魔法字节前 4 字节) + 原始文件」底层还原（doc/docx/xls/xlsx 均实测可完整打开）；
  3. **Excel（`package` 流）** → 直接导出，扩展名按内容魔数判断：zip=`.xlsx`、OLE2=`.xls`（避免旧版 .xls 被误命名 .xlsx 导致 Excel 无法打开）；
- **形状 ↔ ObjectPool 对应规律（实测关键）**：原版手工文档（全 Package/Excel package 流）**正序**对应；本工具输出（含原生 Word OLE 条目）中 Excel 条目在 ObjectPool 内与形状顺序**相反**（3 份独立生成文档实测一致）→ 检测到 `word_native`/`word_docx` 条目即反转；
- **命名**：优先用 `OLEFormat.IconLabel`（本工具生成时 `AddOLEObject(IconLabel=文件名)` 保留了原始文件名，但 reopen 后 Word 不保证返回，故不可靠），否则 Package 用包内文件名、Excel 无原始名时用「考评项名_材料N.xlsx」（如 `隐患排查_材料1.xlsx`，便于在界面识别归属项）；
- **材料落盘**：默认提取到「输出目录\提取材料」；
- **依赖**：olefile（0.47，已加入构建环境与 spec hiddenimports）。

> 附注：v1.2.0 曾加入「读取履职履责表 xlsx 评分」（`read_eval_scores`，openpyxl），经用户确认一度改为读取已生成 .doc 考评表；**v1.1.4 已恢复**并接入界面「② 材料导入（可选）→ 履职履责表」，按姓名回填 12 项自评得分与扣分说明（多姓名时弹出选择框）。

### 3.5 生成流程（时序）

1. GUI 校验：姓名非空；年份 2000~2100；月份 1~12；模板路径有效；自评/上级得分在 0~标准分（兼容「分」后缀）；
2. 组装 `items` 字典（12 项）；
3. 后台线程：`find_template` → `_new_word_app`（不可见）→ `Documents.Open`（模板）→ 结构校验 → `_fill_header`（姓名/月份）→ 逐项填充 C6/C7/C8/C9/C10 → 合计 → C8 对象尺寸兜底缩放 → `SaveAs2(输出, 0)`（Word 2007 自动回退 `SaveAs`）→ 关闭退出；
4. 主线程：进度条 10→88（12 项逐项回调）→ 100；弹窗「打开文件 / 打开文件夹」；写回配置；记录日志。

### 3.6 异常与边界处理

| 场景 | 处理 |
|------|------|
| 模板缺失 | 报错并列出应放置的目录（exe 同目录 / 上一级） |
| 模板结构异常 / 表头异常 | 明确报错，提示更换正确模板 |
| 得分非法 / 超范围 | GUI 逐项提示，拦截生成 |
| 文件重名 | 询问是否覆盖 |
| 材料插入失败 | 退化为文件名文字 |
| Word 弹窗 | `DisplayAlerts=0` 抑制 |
| 配置损坏 | 回退默认值并重建 |
| 生成异常 | 日志 + 弹窗，`finally` 清理 Word 进程 |

### 3.7 测试方案

- `tests/test_core.py`：6 项纯函数测试（命名/表头区间替换/合计/图片识别/权重匹配/文件夹扫描），不依赖 Word，`python tests/test_core.py` 直接运行；
- `tests/test_gui.py`：GUI 冒烟测试（材料增删去重、预览绑定、布局、得分校验、后台扫描异步回填、权重配置、清空、生成完成状态），monkeypatch 屏蔽真实对话框，不启动 Word；
- 手工回归：真实月度数据端到端生成，与人工件逐格比对（结构/字体/图片/合计）。

### 3.8 遗留问题与待办（自《后续开发备忘》并入）

| 待办 | 说明 | 状态 |
|------|------|------|
| 重新打包 exe（32/64 位） | v1.1.3 尚未打包发布；执行 `python _build_win7.py`（32 位）与 `PYI_BITS=64` 的 64 位版 | ⬜ 待发布 |
| 读取材料的落盘目录按「姓名+月份」分目录 | 当前固定提取到「输出目录\提取材料」，多次读取会混在一起，建议按 `姓名+月份` 建子目录 | ⬜ 可选项 |
| 读取图片导出分辨率 | 当前导出为显示分辨率（约 112px），作占位参考够用；如需原分辨率需解析 .doc 的 `Data` 流（较复杂，一般没必要） | ⬜ 可选项 |

## 四、多岗位扩展设计（作业长 / 班组长 / 主要负责人）

### 4.1 扩展动机与目标

未来需为**作业长、班组长、主要负责人**等岗位制作同类《履职清单考评表》生成工具。各岗位差异仅在**模板版式、考评项数量/名称/标准分、行映射、表头标签、命名规则、文件名权重**，核心流程（扫描匹配 → GUI 录入 → Word 填充 → 合计 → 保存）完全一致。目标：**一套框架、多岗位实例，新增岗位零改核心算法**。

### 4.2 岗位参数模型（RoleConfig）

当前实现将岗位参数散落在代码中（下表「位置」列）。扩展第一步是把它们收敛为一个配置对象（v2.0 落地为 JSON）：

```python
# 示意结构（重构目标）
ROLE_CONFIG = {
    "role_id": "安全员",                           # 岗位标识
    "template_name": "安全员安全生产责任制履职清单考评表（模板）.doc",
    "name_pattern": "安全员安全生产责任制履职清单考评表({X}月{XXX}).doc",
    "header_role_label": "考评对象（安全员）：",      # 表头姓名前标签
    "header_next_label": "管理者姓名",               # 姓名区间后标签
    "month_label": "评价月份：",                     # 月份前标签
    "month_next_label": "评价人员签字",              # 月份区间后标签
    "total_rows": 16, "header_row": 1,
    "item_rows": {1: 3, 2: 5, 3: 6, 4: 7, 5: 8, 6: 9,
                  7: 10, 8: 11, 9: 12, 10: 13, 11: 14, 12: 15},
    "item_labels": [(1, "安全绩效", 20), (2, "管理体系", 10),
                    (3, "教育培训", 5), (4, "会议活动", 5),
                    (5, "检查改进", 5), (6, "应急演习", 5),
                    (7, "监督执行", 5), (8, "安全能力", 5),
                    (9, "危险源", 5), (10, "隐患排查", 5),
                    (11, "违章管理", 20), (12, "其它", 10)],
    "col_map": {"desc": 6, "score": 7, "material": 8,
                "eval_desc": 9, "eval_score": 10},
    "material_rules_file": "material_rules.json",    # 岗位独立文件名权重
}
```

### 4.3 演进路线（三步走）

| 步骤 | 方案 | 说明 | 建议 |
|------|------|------|------|
| ① 复制改参（当前即可用） | 复制 `generate_kaoping.py` + `kaoping_core.py`，替换岗位参数后重新打包 | 改动快、零重构风险；多岗位后重复代码多 | 用于快速交付首个新岗位 |
| ② 配置驱动（推荐目标 v2.0） | 4.2 的 `ROLE_CONFIG` 落到 JSON；core 与 GUI 全部读取配置；启动参数 `--role 岗位` 选择实例；`material_rules_{role}.json` 分岗位维护 | 一套代码多岗位，新增岗位 = 新增配置 + 模板 | 作为主推目标 |
| ③ 数据打通（远期） | 恢复读取《履职履责表.xlsx》自动回填自评得分/描述；支持一人/多人批量生成 | 效率最大化 | 视业务需要 |

### 4.4 各岗位需求收集清单（待业务方确认）

> 每个新岗位派生前需提供以下材料（安全员已全部确定）：

| 岗位 | 空白模板(.doc) | 考评项清单/标准分 | 行映射 | 表头标签 | 命名模板 | 文件名权重 |
|------|----------------|-------------------|--------|----------|----------|------------|
| 安全员 | ✅ 已有 | ✅ 12 项 | ✅ R3/R5~R15 | ✅ | ✅ | ✅ |
| 作业长 | ⬜ 待提供 | ⬜ 待确认 | ⬜ | ⬜ | ⬜ | ⬜ |
| 班组长 | ⬜ 待提供 | ⬜ 待确认 | ⬜ | ⬜ | ⬜ | ⬜ |
| 主要负责人 | ⬜ 待提供 | ⬜ 待确认 | ⬜ | ⬜ | ⬜ | ⬜ |

**派生流程**（建议）：
1. 业务方提供新岗位空白模板 → 解析行列结构、合并单元格、考评项所在行（Word COM 脚本化）；
2. 按 4.2 生成该岗位 RoleConfig；
3. 建立 `material_rules_{role}.json`（参考安全员关键词归纳法）；
4. 以 `--role 岗位` 启动加载配置 → 跑自动化测试 → 打包（exe 名 = 岗位名）。

### 4.5 兼容性保障（回归策略）

- core 层保持纯函数、不依赖 GUI，参数化改造只动「取数方式」不动「算法」，现有 6 项核心测试应原样通过；
- GUI 测试补充「按角色加载配置」用例；
- 每个岗位版本沿用 Win7 构建链路（`_build_win7.py`），新增岗位复制 spec 改 EXE_NAME 即可。

---

## 五、构建与交付

### 5.1 打包

- `_build_win7.py` + `安全员履职考评表生成工具.spec`（PyInstaller 5.13.2 / Python 3.8.10）；
- spec 内置 UCRT（`ucrtbase.dll` + downlevel `api-ms-win-*.dll`）→ 纯净 Win7 SP1 免补丁直接运行；
- `upx=False`（避免杀软误报）、`console=False`、模板外置不打包；
- 默认构建 32 位版（32/64 位系统通用，老 Win7 首选）；`PYI_BITS=64` 构建 64 位版。

### 5.2 交付物清单

| 交付物 | 说明 |
|--------|------|
| `dist\安全员履职考评表生成工具.exe` | 64 位免安装单文件 |
| `dist\安全员履职考评表生成工具_win32.exe` | 32 位免安装单文件（32/64 位系统均可运行） |
| `安全员安全生产责任制履职清单考评表（模板）.doc` | 外置模板（与 exe 同目录分发） |
| `使用说明.md` / `README.md` | 用户操作手册 / 项目说明 |
| `技术方案.md` | 本方案 |

---

## 六、风险与注意事项

1. **格式保真**：所有文本用 Range 替换法，禁止重建段落/Run；模板改版后先核对 `ITEM_ROWS` 与表头标签再上线；
2. **合并单元格**：一律 `Cell(r,c)`，禁止 `Rows(i)`；
3. **Word 进程残留**：`finally` 中 Close/Quit，异常路径同样清理；
4. **动态分派挂起**：优先 `gencache.EnsureDispatch`；
5. **图片撑高行**：2cm 上限已控制页数，若模板列宽变化需同步调整 `IMG_MAX_SIZE_CM`；
6. **模板只读**：生成用副本，绝不写回原件；
7. **编码**：源码 UTF-8；日志 FileHandler 显式指定 utf-8（兼容 Py3.8）；
8. **杀软误报**：upx 已关闭；
9. **多岗位维护**：配置驱动落地前新岗位走「复制改参」，注意 core 与测试同步更新。

---

## 七、里程碑与待办

| 阶段 | 内容 | 状态 |
|------|------|------|
| M1 | 技术评审（Q1~Q10） | ✅ |
| M2 | core 层（Word COM 填充：文本/图片/OLE/合计/保存） | ✅ |
| M3 | GUI（12 项表单 + 拖拽 + 预览 + 权重匹配 + 滚轮 + 一键生成 + 后台扫描/进度/日志） | ✅ |
| M4 | 真实数据端到端验证，与人工件对比 | ✅ |
| M5 | PyInstaller 打包单 EXE + 使用说明 | ✅ |
| M6 | v1.1.0 Win7 兼容（内置 UCRT、32/64 位双版本） | ✅ |
| M7 | 岗位参数化（RoleConfig 落 JSON、配置驱动、`--role` 启动） | ⬜ v2.0 规划 |
| M8 | 作业长 / 班组长 / 主要负责人模板收集与派生 | ⬜ 待业务方提供模板 |
| M9 | v1.2.0：读取履职履责表 xlsx 自动回填评分（openpyxl 依赖已入构建环境） | ✅ 已完成（用户确认改为读 .doc，此功能转为备用） |
| M10 | v1.1.2：读取已生成考评表 .doc（评分/评价/支撑材料提取还原）回填界面复用 | ✅ 已完成（olefile 依赖已入构建环境） |
| M11 | v1.1.3：界面易用性优化（分区布局 / 字段对齐与得分范围标注 / 悬停提示 / 统一配色 / 文案精简） | ✅ 已完成 |
| M12 | v1.1.4：读取履职履责表 xlsx 回填评分；修复提取 xlsx 无法打开/文件名丢失；月份从文件名兜底 | ✅ 已完成 |

---

> 附录：v0.1《技术报告_安全员履职考评表生成工具.md》（初期评审稿）与《后续开发备忘.md》的内容已并入本文（模板结构见 2.1、xlsx 关联见 2.2、读取复用见 3.4.12、遗留待办见 3.8），历史文件已移除；本方案为唯一正式技术文档。



