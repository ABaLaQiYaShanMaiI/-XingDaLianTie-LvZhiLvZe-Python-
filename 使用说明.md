# 安全生产责任制履职清单考评表 自动生成工具 使用说明

覆盖四个岗位：安全员 v1.1.5 / 班组长 v2.0.1 / 作业长 v3.0.1 / 主要负责人 v4.0.1。界面与操作流程四岗位完全一致，差异仅在考评项清单、模板文件、命名模板与履职履责表 sheet（见「二、各岗位参数速查表」）。

## 一、运行环境

- **操作系统**：Windows 7 SP1 ~ Windows 11（32 位 / 64 位均支持；exe 已内置通用 C 运行时 UCRT，纯净未打补丁的 Win7 也可直接运行）
- **必需软件**：Microsoft Word 2010 及以上（程序通过 Word 引擎生成 .doc 文件；Word 2007 亦有兼容兜底）
- **使用方式**：
  - 方式 A（开发/源码运行）：进入对应岗位目录，运行 `generate_kaoping.py`（需 Python 3.8.10，依赖见文末）；
  - 方式 B（最终用户）：直接双击对应岗位 `dist\` 下 exe（免安装、免 Python 环境；模板文件外置，需与 exe 放在同一目录，见「六、模板文件」）。
- **各岗位 exe 命名**：
  - 安全员：`安全员履职考评表生成工具.exe`（64 位）/ `安全员履职考评表生成工具_win32.exe`（32 位）
  - 班组长：`班组长履职考评表生成工具.exe` / `班组长履职考评表生成工具_win32.exe`
  - 作业长：`作业长履职考评表生成工具.exe` / `作业长履职考评表生成工具_win32.exe`
  - 主要负责人：`主要负责人履职考评表生成工具.exe` / `主要负责人履职考评表生成工具_win32.exe`
  - 拿不准系统位数时，用 `_win32` 版最稳妥（32 位程序可在 64 位系统上正常跑）。

## 二、各岗位参数速查表

| 岗位 | 版本 | 考评项数 | 模板文件 | 默认命名模板 | 履职履责表 sheet | 界面标题 |
|------|------|----------|----------|--------------|------------------|----------|
| 安全员 | v1.1.5 | 12 | 安全员安全生产责任制履职清单考评表（模板）.doc | 安全员安全生产责任制履职清单考评表({X}月{XXX}).doc | 安全员月度履职评价表 | 安全员…自动生成工具 |
| 班组长 | v2.0.1 | 17 | 班组长安全生产责任制履职清单考评表（模板）.doc | 班组长安全生产责任制履职清单考评表({X}月{XXX}).doc | 班组长月度履职评价表 | 班组长…自动生成工具 |
| 作业长 | v3.0.1 | 13 | 作业长安全生产责任制履职履责考评表（模板）.doc | 作业长安全生产责任制履职履责考评表({X}月{XXX}).doc | 作业长月度履职评价表 | 作业长…自动生成工具 |
| 主要负责人 | v4.0.1 | 13 | 主要责任人安全生产责任制履职清单考评表（模板）.doc | 主要负责人安全生产责任制履职清单考评表({X}月{XXX}).doc | 主要负责人月度履职评价表 | 主要负责人…自动生成工具 |

> 表头标签均为「考评对象（岗位名）：…评价月份：…」；**主要负责人**模板表头无「管理者姓名」栏（考评对象（主要负责人）后直接是评价月份），程序已适配。

## 三、界面操作流程

### 1. 顶部「基本信息」

| 项 | 说明 |
|----|------|
| 年份 / 月份 | 生成文件与表头「评价月份」所用（默认当前年/月） |
| 姓名 | 手填（生成文件表头「考评对象」所用） |
| 命名模板 | 文件名规则，占位符：`{Y}`=年、`{X}`=月、`{XXX}`=姓名（默认见速查表） |
| 模板文件 | 默认自动定位 exe/程序同目录或其上一级目录下的对应岗位模板；可点击/拖入更换 |
| 输出目录 | 生成文件保存位置（记忆上次路径） |
| 支撑材料文件夹 | **点击/拖入选择当月文件夹，或直接选年度根目录**（如 `E:\2.武钢兴达工作\2.月度固定工作\2026`，程序按界面年份/月份自动定位当月子文件夹如 `2026.08`）。程序按**文件名权重**自动匹配填入各考评项，匹配分越高排在越前；未匹配的文件会提示，可手动拖入对应项 |
| 文件名权重 | 关键词表放在对应岗位 exe/程序同目录的 `material_rules.json`（**首次运行自动生成**），每个考评项关键词：第 1 词权重 2、其余权重 1。点「编辑权重」按钮用记事本打开，按业务习惯增删关键词，**保存后重启程序生效** |
| 读取考评表 | **点击/拖入已生成的考评表(.doc)**，自动读取**姓名/月份、各考评项「自评描述/自评得分/评价描述/上级评分」与支撑材料**（图片、Excel/Word 等内嵌文件全部提取还原）到界面；支撑材料提取到「输出目录\提取材料\姓名月份」子目录（如 `提取材料\孙忠8月`，按姓名+月份分目录避免混在一起），可双击预览、右键删除、拖入替换，改月份后即可一键生成新表 |
| 履职履责表(.xlsx) | **点击/拖入月度履职履责表.xlsx**，按对应岗位 sheet 自动读取所选人员的各项**自评得分**填入界面（**excel 表只提供姓名和分数**，不读取"扣分说明"列；表头匹配以模板考评项名称为准）；文件含多个姓名时会弹出姓名选择框 |

### 2. 中部「各考评项」

每项可填写：

- **自评描述**：该项履职情况文字说明；
- **自评得分**：默认等于标准分，有扣分时改为实际得分；
- **材料说明**：自评实绩材料列的补充文字（可留空）；
- **支撑材料**：**拖拽任意文件**（图片 / Word / Excel / PDF / 照片等，每格最多 15 个）到蓝色区域（或点击"选择材料"）；
  - 图片 → 显示**缩略图**；其他文件 → 显示 **📄 文件名**；
  - **双击预览打开**材料源文件，鼠标悬停显示完整文件名；
  - **右键单击预览删除该材料**（仅从列表移除，源文件不受影响，可重新拖入）；「清空材料」按钮删除该项全部材料；
- **评价描述 / 上级评分**：评价人员填写部分（可留空，留空则交回人工手签）。

版式差异：

- **班组长**：第 1 项「安全绩效」在模板中占两行、每行独立填写，界面第 1 项显示「第 1 行 / 第 2 行」两组输入（20 分拆 10+10，可在代码 `SUB_ROW_MAX` 调整），合计把两行得分一并求和；
- **作业长 / 主要负责人**：各考评项填写格（C6~C10）与续行纵向合并，界面每项一组输入即可覆盖整个考评块；主要负责人第 11/12 项续行自评描述(C6)为逐行独立格，生成时填写主行即可，续行描述可按需手工补充。

中部列表支持**鼠标滚轮滚动**（鼠标悬停在任意一项上滚动即可）。

### 3. 底部工具栏

- **支撑材料文件夹（顶部蓝色区域）**：**拖入/点击选择当月文件夹或年度根目录**，自动按文件名权重匹配支撑材料填入各考评项（匹配分高的排前面）；「编辑权重」按钮可打开对应岗位 `material_rules.json` 调整关键词；
- **清空全部**：清空所有填写内容；
- **一键生成**：校验（姓名/年月范围/得分范围/模板结构）后**在后台自动打开 Word 填入** → 生成对应岗位考评表，期间界面不卡顿，**进度条逐项推进、完成后显示耗时**，并询问"打开文件 / 打开文件夹"（「一键生成」固定在右下角，窗口缩小也不被遮挡）。对应岗位目录会生成 `kaoping.log` 日志，便于排障。
- **上级评分/评语自动对应**：某考评项填了自评得分后，「一键生成」时自动生成与之对应的**上级评分（=自评得分）**与**评语（=已完成）**；已手工填写的上级评分/评语不覆盖；未填自评得分的项不生成。

## 四、各岗位考评项清单

**安全员（12 项，合计 100 分）**：①安全绩效(20) ②管理体系(10) ③教育培训(5) ④会议活动(5) ⑤检查改进(5) ⑥应急演习(5) ⑦监督执行(5) ⑧安全能力(5) ⑨危险源(5) ⑩隐患排查(5) ⑪违章管理(20) ⑫其它(10)。

**班组长（17 项，合计 100 分）**：①安全绩效(20，双行) ②履职评价(5) ③违章检查(5) ④劳防及工机具(5) ⑤教育培训(5) ⑥会议活动(5) ⑦检查改进(5) ⑧应急处置(5) ⑨班前活动(5) ⑩安全隐患(5) ⑪安全交底(5) ⑫安全标志(5) ⑬作业许可(5) ⑭防火防爆(5) ⑮治安管理(5) ⑯基础工作(5) ⑰安全技能(5)。

**作业长（13 项，合计 100 分）**：①安全绩效(20) ②隐患排查(5) ③安全检查(5) ④安全教育(5) ⑤协力安全(5) ⑥危险源管理(5) ⑦危化品(5) ⑧应急管理(5) ⑨违章查处(20) ⑩工机具(5) ⑪劳防用品(5) ⑫履职评价(5) ⑬"六个一"工作实绩(10)。

**主要负责人（13 项，合计 100 分）**：①安全绩效(20) ②隐患排查(5) ③安全检查(5) ④安全教育(5) ⑤危险源管理(5) ⑥应急管理(5) ⑦违章查处(10) ⑧机构人员(5) ⑨制度规程(5) ⑩费用投入(5) ⑪履职评价(5) ⑫"六个一"工作实绩(10) ⑬甲方契合度(15)。


## 五、生成结果说明

- 生成文件为 `.doc` 格式，与手工件版式完全一致（宋体10pt 单元格、黑体12pt 表头）；
- 表头自动填入：考评对象姓名、评价月份；
- 各考评项自动填入：自评描述（C6）、自评得分（C7）、自评实绩材料（C8 文字+材料）、评价描述（C9）、上级评分（C10）；
- 合计行自动求和（C6 自评合计、C9 上级合计；班组长第 1 项两行得分一并计入）；
- **支撑材料呈现方式**：
  - 图片 → 内嵌图片，自动等比缩放，**长宽均不超过 2cm**；
  - 其他文件 → 嵌入为 **OLE 对象（图标+文件名），双击可打开源文件**（Word 标准"插入对象"效果），显示尺寸同样自动缩小到**长宽 2cm 以内**；个别无法嵌入的文件类型会自动退化为"文件名文字"。

## 六、模板文件

- 各岗位模板由甲方提供，程序**只读不写**，生成时使用副本操作，模板原件安全；
- **模板为外置文件，不内置进 exe**。程序按以下顺序自动定位：
  1. 界面上手动选择/拖入的模板路径；
  2. exe 所在目录（源码运行时为脚本所在目录）；
  3. exe 所在目录的上一级目录。
- 分发 exe 时，请将对应岗位模板与 exe **放在同一目录**：
  - 安全员：`安全员安全生产责任制履职清单考评表（模板）.doc`
  - 班组长：`班组长安全生产责任制履职清单考评表（模板）.doc`
  - 作业长：`作业长安全生产责任制履职履责考评表（模板）.doc`
  - 主要负责人：`主要责任人安全生产责任制履职清单考评表（模板）.doc`
- 若模板结构有变（如行号变化），请告知开发者同步更新对应岗位 `kaoping_core.py` 中的 `ITEM_ROWS` 映射。

## 七、常见问题

| 问题 | 处理 |
|------|------|
| 提示"找不到模板文件" | 将对应岗位模板放到 exe/程序同目录（或其上一级目录），或点击"模板文件-选择"手动指定 |
| 提示"模板表格结构异常" | 模板可能被替换/改版（各岗位期望行列见「二、各岗位参数速查表」），请换回正确模板 |
| 支撑材料自动匹配不准 | 打开「编辑权重」按钮编辑对应岗位 `material_rules.json`（各月同义文件夹名可一并加上）；保存后重启程序生效；未匹配文件请手动拖入对应项 |
| 履职履责表读取提示格式变化 | 各岗位月度 xlsx 版式以业务方当月文件为准；**xlsx 表头匹配以模板考评项名称为准，excel 只提供姓名和分数（不读"扣分说明"）**；已用 2026.07（炼铁保产事业部 / 兴达炼铁保产事业部）真实文件验证四岗位可正确读取（含多姓名文件弹出选择框）；若后续表头用词再变，请以模板为准统一或反馈开发者调整 `XLSX_ITEM_KEYWORDS` |
| Word 未安装 | 程序需依赖 Microsoft Word，请先安装 |
| 生成的 .doc 特别大（几十 MB） | 支撑材料以 **OLE 整文件嵌入**是需求所限：Excel/Word 等源文件体积会直接计入 .doc（实测嵌入一个 xlsx 后 .doc 可膨胀到数十 MB）。图片程序已自动压缩，但**请尽量精简材料源文件**（删除历史版本、清空无用 sheet），生成的 .doc 才小巧 |
| 生成时 Word 有弹窗 | 程序已关闭 Word 提示（DisplayAlerts=0），若首次使用请允许 Word 初始化 |

## 八、开发环境依赖（方式 A）

兼容 Win7~Win11 的构建环境（版本已锁定，请勿随意升级）：

```
Python    3.8.10          # 官方支持 Win7 的最后版本（用 32 位版可构建 _win32 exe）
pip install -r requirements.txt   # 依赖已锁版本（仓库根目录）
```

打包命令（进入对应岗位目录，spec 已内置 UCRT）：

```
# 32 位版（32/64 位系统均可运行，老 Win7 首选）
python _build_win7.py                          # 默认 PYI_BITS=32

# 64 位版（64 位系统专用）
$env:PYI_BITS="64"; $env:PYI_EXE_NAME="<岗位>履职考评表生成工具"
python _build_win7.py
```

> 说明：
> - `_build_win7.py` 调用 PyInstaller 5.13.2 执行对应岗位的 `.spec`；
> - spec 通过 `binaries` 把 Win7 运行时整套文件打进 exe：`ucrtbase.dll` + downlevel 目录全部
>   `api-ms-win-*.dll`（含 api-ms-win-crt-* 与 api-ms-win-core-* 等 API-Set 转发器），
>   纯净 Win7 免装补丁，且不会出现「丢失 api-ms-win-core-sysinfo-l1-2-0.dll」类报错；
> - 构建中间文件放在 %TEMP% 下，不占用项目目录；产物输出到对应岗位 `dist\`。

