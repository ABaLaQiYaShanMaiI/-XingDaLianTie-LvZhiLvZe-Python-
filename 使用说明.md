# 安全员安全生产责任制履职清单考评表 自动生成工具 使用说明 v1.0.0

## 一、运行环境

- **操作系统**：Windows 10/11
- **必需软件**：Microsoft Word 2010 及以上（程序通过 Word 引擎生成 .doc 文件）
- **使用方式**：
  - 方式 A（开发/源码运行）：本目录下 `generate_kaoping.py`（需 Python 3.10+，依赖见文末）；
  - 方式 B（最终用户）：直接双击 `dist\安全员履职考评表生成工具.exe`（免安装，绿色单文件）。

## 二、界面操作流程

### 1. 顶部「基本信息」
| 项 | 说明 |
|----|------|
| 年份 / 月份 | 生成文件与表头「评价月份」所用（默认当前年/月） |
| 姓名 | 可手填；点击「读取履职履责表.xlsx」后变为下拉选择 |
| 命名模板 | 文件名规则，占位符：`{Y}`=年、`{X}`=月、`{XXX}`=姓名（默认 `安全员安全生产责任制履职清单考评表({X}月{XXX}).doc`） |
| 模板文件 | 默认自动定位同目录 `安全员安全生产责任制履职清单考评表（模板）.doc`；可点击/拖入更换 |
| 输出目录 | 生成文件保存位置（记忆上次路径） |

### 2. 中部「12 个考评项」
每项可填写：
- **自评描述**：该项履职情况文字说明；
- **自评得分**：默认等于标准分，有扣分时改为实际得分；
- **材料说明**：自评实绩材料列的补充文字（可留空）；
- **支撑材料**：**拖拽任意文件**（图片 / Word / Excel / PDF / 照片等，每格最多 15 个）到蓝色区域（或点击"选择材料"）；
  - 图片 → 显示**缩略图**；
  - 其他文件 → 显示 **📄 文件名**；
  - **点击预览可移除**该材料，鼠标悬停显示完整文件名；
- **评价描述 / 上级评分**：评价人员填写部分（可留空，留空则交回人工手签）。

### 3. 底部工具栏
- **履职履责表（顶部绿色区域）**：**拖入当月 `履职履责表（兴达）.xlsx` 到此自动填充**，或点击选择；读取「安全员月度履职评价表」sheet 后，自动填入 12 项自评得分与扣分说明，姓名下拉列出全部安全员；
- **清空全部**：清空所有填写内容；
- **一键生成**：校验后自动打开 Word 填入 → 生成 `安全员安全生产责任制履职清单考评表(X月XXX).doc` → 完成后询问"打开文件 / 打开文件夹"。

## 三、生成结果说明

- 生成文件为 `.doc` 格式，与手工件版式完全一致（宋体10pt 单元格、黑体12pt 表头）；
- 表头自动填入：考评对象姓名、评价月份；
- 12 项自动填入：自评描述（C6）、自评得分（C7）、自评实绩材料（C8 文字+材料）、评价描述（C9）、上级评分（C10）；
- 合计行自动求和（C6 自评合计、C9 上级合计）；
- **支撑材料呈现方式**：
  - 图片 → 内嵌图片，自动缩放到宽约 2.5cm 并按比例；
  - 其他文件 → 嵌入为 **OLE 对象（图标+文件名），双击可打开源文件**（Word 标准"插入对象"效果）；个别无法嵌入的文件类型会自动退化为"文件名文字"。

## 四、模板文件

- 模板文件 `安全员安全生产责任制履职清单考评表（模板）.doc` 由甲方提供，本程序**只读不写**，生成时使用副本操作，模板原件安全。
- 若模板结构有变（如行号变化），请告知开发者同步更新 `kaoping_core.py` 中的 `ITEM_ROWS` 映射。

## 五、常见问题

| 问题 | 处理 |
|------|------|
| 提示"找不到模板文件" | 将模板 `安全员安全生产责任制履职清单考评表（模板）.doc` 放到 exe/程序同目录，或点击"模板文件-选择"手动指定 |
| 提示"xlsx 中找不到工作表" | 该 xlsx 必须含「安全员月度履职评价表」sheet（各月兴达版履职履责表均有） |
| Word 未安装 | 程序需依赖 Microsoft Word，请先安装 |
| 生成时 Word 有弹窗 | 程序已关闭 Word 提示（DisplayAlerts=0），若首次使用请允许 Word 初始化 |

## 六、开发环境依赖（方式 A）

```
pip install pywin32 python-docx openpyxl pillow tkinterdnd2 pyinstaller
```
打包命令：`python -m PyInstaller --clean -y 安全员履职考评表生成工具.spec`
