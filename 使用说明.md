# 安全员安全生产责任制履职清单考评表 自动生成工具 使用说明 v1.1.2

## 一、运行环境

- **操作系统**：Windows 7 SP1 ~ Windows 11（32 位 / 64 位均支持；exe 已内置通用 C 运行时 UCRT，纯净未打补丁的 Win7 也可直接运行）
- **必需软件**：Microsoft Word 2010 及以上（程序通过 Word 引擎生成 .doc 文件；Word 2007 亦有兼容兜底）
- **使用方式**：
  - 方式 A（开发/源码运行）：本目录下 `generate_kaoping.py`（需 Python 3.8.10，依赖见文末）；
  - 方式 B（最终用户）：直接双击 `dist\` 下 exe（免安装、免 Python 环境；模板文件外置，需与 exe 放在同一目录，见「四、模板文件」）。
- **两个 exe 怎么选**（v1.1.0 起）：
  - `安全员履职考评表生成工具.exe`：**64 位版**，用于 64 位系统（Win7 x64 ~ Win11）；
  - `安全员履职考评表生成工具_win32.exe`：**32 位版**，32 位与 64 位系统均可运行（老 32 位 Win7 首选）；
  - 拿不准系统位数时，用 `_win32` 版最稳妥（32 位程序可在 64 位系统上正常跑）。

## 二、界面操作流程

### 1. 顶部「基本信息」
| 项 | 说明 |
|----|------|
| 年份 / 月份 | 生成文件与表头「评价月份」所用（默认当前年/月） |
| 姓名 | 手填（生成文件表头「考评对象」所用） |
| 命名模板 | 文件名规则，占位符：`{Y}`=年、`{X}`=月、`{XXX}`=姓名（默认 `安全员安全生产责任制履职清单考评表({X}月{XXX}).doc`） |
| 模板文件 | 默认自动定位 exe/程序同目录或其上一级目录下的 `安全员安全生产责任制履职清单考评表（模板）.doc`；可点击/拖入更换 |
| 输出目录 | 生成文件保存位置（记忆上次路径） |
| 支撑材料文件夹 | **点击/拖入选择当月文件夹，或直接选年度根目录**（如 `E:\2.武钢兴达工作\2.月度固定工作\2026`，程序按界面年份/月份自动定位当月子文件夹如 `2026.08`）。程序按**文件名权重**自动匹配填入 12 个考评项（如「扫雷→⑩隐患排查、综合大检查→⑤检查改进、应急演练→⑥应急演习、安监网/安检网录入→⑪违章管理、X月主题工作→⑫其它」等），匹配分越高排在越前；未匹配的文件会提示，可手动拖入对应项 |
| 文件名权重 | 关键词表放在 exe/程序同目录的 `material_rules.json`（**首次运行自动生成**），每个考评项关键词：第 1 词权重 2、其余权重 1。点「编辑权重」按钮用记事本打开，按业务习惯增删关键词（如把某月同义文件夹名都加进去），**保存后重启程序生效** |
| 履职履责表 | **点击/拖入已生成的考评表(.doc)**（如 `安全员安全生产责任制履职清单考评表（孙忠7月）.doc`），自动读取**姓名/月份、12 项「自评描述/自评得分/评价描述/上级评分」与支撑材料**（图片、Excel/Word 等内嵌文件全部提取还原）到界面；支撑材料提取到「输出目录\提取材料」，可双击预览、右键删除、拖入替换，改月份后即可一键生成新表 |

### 2. 中部「12 个考评项」
每项可填写：
- **自评描述**：该项履职情况文字说明；
- **自评得分**：默认等于标准分，有扣分时改为实际得分；
- **材料说明**：自评实绩材料列的补充文字（可留空）；
- **支撑材料**：**拖拽任意文件**（图片 / Word / Excel / PDF / 照片等，每格最多 15 个）到蓝色区域（或点击"选择材料"）；
  - 图片 → 显示**缩略图**；
  - 其他文件 → 显示 **📄 文件名**；
  - **双击预览打开**材料源文件，鼠标悬停显示完整文件名；
  - **右键单击预览删除该材料**（仅从列表移除，源文件不受影响，可重新拖入）；「清空材料」按钮删除该项全部材料；
- **评价描述 / 上级评分**：评价人员填写部分（可留空，留空则交回人工手签）。
- 中部列表支持**鼠标滚轮滚动**（鼠标悬停在任意一项上滚动即可）。

### 3. 底部工具栏
- **支撑材料文件夹（顶部蓝色区域）**：**拖入/点击选择当月文件夹或年度根目录**，自动按文件名权重匹配支撑材料填入 12 个考评项（匹配分高的排前面）；「编辑权重」按钮可打开本地 `material_rules.json` 调整关键词；
- **清空全部**：清空所有填写内容；
- **一键生成**：校验（姓名/年月范围/得分范围/模板结构）后**在后台自动打开 Word 填入** → 生成 `安全员安全生产责任制履职清单考评表(X月XXX).doc`，期间界面不卡顿，**进度条逐项推进、完成后显示耗时**，并询问"打开文件 / 打开文件夹"（「一键生成」固定在右下角，窗口缩小也不被遮挡）。程序同目录会生成 `kaoping.log` 日志，便于排障。

## 三、生成结果说明

- 生成文件为 `.doc` 格式，与手工件版式完全一致（宋体10pt 单元格、黑体12pt 表头）；
- 表头自动填入：考评对象姓名、评价月份；
- 12 项自动填入：自评描述（C6）、自评得分（C7）、自评实绩材料（C8 文字+材料）、评价描述（C9）、上级评分（C10）；
- 合计行自动求和（C6 自评合计、C9 上级合计）；
- **支撑材料呈现方式**：
  - 图片 → 内嵌图片，自动等比缩放，**长宽均不超过 2cm**；
  - 其他文件 → 嵌入为 **OLE 对象（图标+文件名），双击可打开源文件**（Word 标准"插入对象"效果），显示尺寸同样自动缩小到**长宽 2cm 以内**；个别无法嵌入的文件类型会自动退化为"文件名文字"。

## 四、模板文件

- 模板文件 `安全员安全生产责任制履职清单考评表（模板）.doc` 由甲方提供，本程序**只读不写**，生成时使用副本操作，模板原件安全。
- **模板为外置文件，不内置进 exe**。程序按以下顺序自动定位：
  1. 界面上手动选择/拖入的模板路径；
  2. exe 所在目录（源码运行时为脚本所在目录）；
  3. exe 所在目录的上一级目录。
- 分发 exe 时，请将模板与 `安全员履职考评表生成工具.exe` **放在同一目录**。
- 若模板结构有变（如行号变化），请告知开发者同步更新 `kaoping_core.py` 中的 `ITEM_ROWS` 映射。

## 五、常见问题

| 问题 | 处理 |
|------|------|
| 提示"找不到模板文件" | 将模板 `安全员安全生产责任制履职清单考评表（模板）.doc` 放到 exe/程序同目录（或其上一级目录），或点击"模板文件-选择"手动指定 |
| 提示"模板表格结构异常" | 模板可能被替换/改版（期望 16 行×10 列），请换回正确模板 |
| 支撑材料自动匹配不准 | 打开「编辑权重」按钮编辑本地 `material_rules.json`（各月同义文件夹名可一并加上，如 `安监网录入` 与 `安检网录入`）；保存后重启程序生效；未匹配文件请手动拖入对应项 |
| Word 未安装 | 程序需依赖 Microsoft Word，请先安装 |
| 生成时 Word 有弹窗 | 程序已关闭 Word 提示（DisplayAlerts=0），若首次使用请允许 Word 初始化 |

## 六、开发环境依赖（方式 A）

兼容 Win7~Win11 的构建环境（版本已锁定，请勿随意升级）：

```
Python    3.8.10          # 官方支持 Win7 的最后版本（用 32 位版可构建 _win32 exe）
pip install pyinstaller==5.13.2 pillow==10.4.0 pywin32==306 tkinterdnd2==0.4.3
```

打包命令（spec 已内置 UCRT，产物即 v1.1.0）：

```
# 32 位版（32/64 位系统均可运行，老 Win7 首选）
python _build_win7.py                          # 默认 PYI_BITS=32

# 64 位版（64 位系统专用）
$env:PYI_BITS="64"; $env:PYI_EXE_NAME="安全员履职考评表生成工具"
python _build_win7.py
```

> 说明：
> - `_build_win7.py` 调用 PyInstaller 5.13.2 执行 `安全员履职考评表生成工具.spec`；
> - spec 通过 `binaries` 把 Win7 运行时整套文件打进 exe：`ucrtbase.dll` + downlevel 目录全部
>   `api-ms-win-*.dll`（含 api-ms-win-crt-* 与 api-ms-win-core-* 等 API-Set 转发器），
>   纯净 Win7 免装补丁，且不会出现「丢失 api-ms-win-core-sysinfo-l1-2-0.dll」类报错；
> - 构建中间文件放在 %TEMP% 下，不占用项目目录；产物输出到 `dist\`。
